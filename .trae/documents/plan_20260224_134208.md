# 添加liblib平台支持到ComfyUI MCP

## 核心修改

### 1. 修改模型下载器配置

在`downloader.py`文件中添加liblib平台的配置：

```python
self.repositories = config.get("repositories", {
    "huggingface": "https://huggingface.co",
    "civitai": "https://civitai.com",
    "liblib": "https://www.liblib.ai"
})
```

### 2. 添加liblib链接处理

在`_download_file`方法中添加对liblib链接的处理：

```python
# Handle different repositories
if "huggingface.co" in url:
    url = self._process_huggingface_url(url)
elif "civitai.com" in url:
    url = self._process_civitai_url(url)
elif "liblib.ai" in url:
    url = self._process_liblib_url(url)
```

### 3. 实现liblib URL处理方法

添加`_process_liblib_url`方法来处理liblib链接：

```python
def _process_liblib_url(self, url):
    """Process liblib URL to get direct download link"""
    # Handle liblib URLs
    # Example: https://www.liblib.ai/model-detail/12345/model-name -> API call to get download link
    match = re.match(r"https://www\.liblib\.ai/model-detail/(\d+)", url)
    if match:
        model_id = match.groups()[0]
        
        # Get model info from liblib API
        api_url = f"https://www.liblib.ai/api/model/{model_id}"
        try:
            response = requests.get(api_url, timeout=10)
            response.raise_for_status()
            data = response.json()
            
            # Get download link
            if "data" in data and "downloadUrl" in data["data"]:
                return data["data"]["downloadUrl"]
        except Exception as e:
            logger.error(f"Failed to get liblib download link: {e}")
    
    return url
```

### 4. 添加liblib模型搜索支持

在`search_models`方法中添加对liblib的搜索支持：

```python
if source in ["all", "liblib"]:
    # Search liblib
    try:
        api_url = "https://www.liblib.ai/api/model/list"
        params = {
            "keyword": query,
            "page": 1,
            "limit": 10
        }
        response = requests.get(api_url, params=params, timeout=10)
        response.raise_for_status()
        data = response.json()
        
        if "data" in data and "list" in data["data"]:
            for model in data["data"]["list"]:
                results.append({
                    "id": str(model.get("id")),
                    "name": model.get("name"),
                    "author": model.get("username"),
                    "description": model.get("description"),
                    "tags": model.get("tags", []),
                    "downloads": model.get("downloadCount"),
                    "likes": model.get("likeCount"),
                    "source": "liblib",
                    "url": f"https://www.liblib.ai/model-detail/{model.get('id')}"
                })
    except Exception as e:
        logger.error(f"Failed to search liblib: {e}")
```

## 配置更新

### 1. 更新默认配置

在`main.py`文件中更新默认配置，添加liblib平台：

```python
return {
    "model_dir": os.path.join(os.path.dirname(__file__), "models"),
    "api_port": 8188,
    "api_host": "localhost",
    "download_timeout": 3600,
    "max_concurrent_downloads": 2,
    "repositories": {
        "huggingface": "https://huggingface.co",
        "civitai": "https://civitai.com",
        "liblib": "https://www.liblib.ai"
    }
}
```

### 2. 更新配置文件

在`config/config.json`文件中添加liblib平台配置：

```json
{
    "model_dir": "path/to/models",
    "api_port": 8188,
    "api_host": "localhost",
    "download_timeout": 3600,
    "max_concurrent_downloads": 2,
    "repositories": {
        "huggingface": "https://huggingface.co",
        "civitai": "https://civitai.com",
        "liblib": "https://www.liblib.ai"
    }
}
```

## 测试计划

1. **链接处理测试**：测试从liblib复制的模型链接是否能正确处理和下载
2. **搜索功能测试**：测试搜索liblib平台上的模型
3. **下载功能测试**：测试从liblib下载模型的完整性和速度
4. **集成测试**：测试liblib平台与其他平台的兼容性

## 预期结果

- ComfyUI MCP现在支持从liblib平台下载模型
- 用户可以搜索和下载liblib上的模型
- 下载速度和稳定性与其他平台相当
- 与现有功能无缝集成

## 注意事项

- 确保liblib API的调用方式正确，可能需要根据实际API文档进行调整
- 处理可能的API速率限制和认证要求
- 确保错误处理的完整性，避免因liblib API变更导致整个下载过程失败
- 保持与其他平台的代码风格和处理逻辑一致